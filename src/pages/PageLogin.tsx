/* TODO(page-builder): implement per docs/SPEC-pages.md "Page: Login".
 * Two-column split. Left = brand + tagline + code block + stats (mock OK).
 * Right = email/api-key tab toggle + form -> loginEmail/loginApiKey -> setAuth.
 * Full-bleed: no Shell wrapper.
 */
export function PageLogin() {
  return (
    <div style={{ position: 'fixed', inset: 0, display: 'grid', placeItems: 'center', color: 'var(--fg-2)' }}>
      Login page — TODO
    </div>
  );
}
