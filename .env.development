# dev: empty base → fetch('/v1/...') hits the vite dev server, which proxies
# to a locally-running API (see vite.config.ts). Same-origin = cookies + no CORS.
# Point the proxy elsewhere with VITE_DEV_API_TARGET if your API listens on
# another address.
VITE_API_BASE=
